{
  "name": "todo_list",
  "version": "0.0.1",
  "description": "A todo list in node.js and Angular",
  "main": "server.js",
  "dependencies": {
    "body-parser": "~1.5.2",
    "express": "~4.7.2",
    "method-override": "~2.1.2",
    "mongodb": "^2.1.16", 
    "mongoose": "^4.4.12",
    "morgan": "~1.2.2"
  },
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd client/ && npm install && npm install --only=dev --no-shrinkwrap && npm run build"
  },
}
